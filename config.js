let token = "Votre token" // Mettez le token de votre compte discord
let prefix = "" // Désignez un préfixe a ajouter au selfbot
let color = "" // Désignez une couleur a ajouter au selfbot (format HTML ou "BLUE", "YELLOW", ...)
let image = "" // Mettez une image sur les embeds du selfbot
let premium = "" // facultatif, entrez votre clée premium du selfbot
let nsfw = "" // Sélectionnez On/Off pour désactiver les commandes de visibilité d'images pornographiques
let mod1 = "" // Ajoutez un membre ayant la possibilité d'utiliser le SelfBot
let mod2 = "" // Ajoutez un deuxième membre ayant la possibilité d'utiliser le SelfBot

module.exports = {
    token,
    prefix,
    color,
    image,
    premium,
    nsfw,
    mod1,
    mod2
}
